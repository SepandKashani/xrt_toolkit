+ DRJIT/ASTRA benchmarks in 3D CBCT setup (CPU, GPU)
  Share with EA; check Element for configuration

+ Add DR test suite

+ Implement FBP/FDK, with choice of filters.
  Supported scan geometries: PBCT, FBCT, CBCT
  Only support xrt_struct-format, hence non-uniform angles ok?

+ RX-solutions: XML ingestor
  - standard XML format
  - advanced XML format



+ Voxel-driven back-projection: difference with ray-based approach?
  Is this beneficial to improve bproj performance?
  If yes, how to implement efficiently in drjit?

+ Add interpolation module to compute (knot \conv box-spline) output on uniform mesh.
  For simplicity we only support the upsampling case where the mesh aligns with the knot space, i.e.

  step_out = step_in / Q, Q \in \bN^{*}
  start_out = start_in + step_out * l, l \in \bZ

  Uses numpy/cupy behind the scenes

+ [1/2] Add NumPy, CuPy, PyTorch, JAX interface

  [x] Interop provided via utils.asarray()
  [ ] add explicit PyTorch/JAX interface with custom vjp() rules?

+ Documentation: add HTML docs on ReadTheDocs
  Include PBCT/CBCT examples

+ [0/2] [drjit.ray_xrt.py] Implement order-1/2 methods (3D)
  - [ ] _order_[12]_project()
  - [ ] _order_[12]_backproject()

  + Use NNs to evaluate box_splines?
    May be faster/equivalent to closed-form expressions, both in 2D and 3D.
    This should be tested in PyTorch beforehand; will be rolled out via drjit.nn if conclusive.

+ [SWE] Python 3.14 available 2025.10.07
  - pyproject.toml: bump maxver
  - tox.ini: bump maxver
    run full test suite.
    Ensure all dependencies support 3.14

+ [SWE] Python 3.10 reaches EOL in 2026.10
  - pyproject.toml: bump minver
  - tox.ini: bump minver
